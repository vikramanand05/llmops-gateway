apiVersion: 1

providers:
  - name: LLMOps Gateway
    orgId: 1
    folder: LLMOps
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
